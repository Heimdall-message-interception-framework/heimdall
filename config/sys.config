[
{ sched_msg_interception_erlang, []},
 {kernel, [
  {logger_level, warning},
    %% Disk logger for schedules
    {handler, disk_log, logger_disk_log_h,
        # {config => #{
            file => "thisiswherewesave.txt",
            type => wrap,
            max_no_files => 10,
            max_no_bytes => 52428800 % 10 x 5mb
         },
         level => warning,
         formatter => {flatlog, #{
           map_depth => 3,
           term_depth => 50
         }}
        }
    }
  ]}
].
